defaultScreen

Adds an icon on the traybar that allows to quickly change the default screen in a multi-monitor environment.

It's written in C++/Qt, but since it makes use of Win32 APIs, it will only run on Windows.
