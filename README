This application should, like, make it easier to set which screen should be the primary one in a multi-monitor environment.



It's written in C++/Qt, but since it makes use of Win32 APIs, it will only run on Windows.
